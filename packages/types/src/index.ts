// Shared types go here

export type HelloMessage = {
    status: string;
}

export type ChangeMeIfNeeded = {
    id: number;
    createdAt: Date;
    updatedAt: Date;
}

export type CreateChangeMeIfNeededRequest = {
    // Extend as needed
}
